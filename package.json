{
  "name": "pipethis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "pipethis": "render.mjs"
  },
  "description": "pipethis — render large pastes/files/clipboard to dense pxpipe PNG image blocks (with a verbatim byte-exact appendix) to cut Claude input tokens. MCP server + CLI + Claude Code skill.",
  "files": [
    "lib.mjs",
    "server.mjs",
    "render.mjs",
    "skill/",
    "install.sh",
    "install.ps1",
    "uninstall.sh",
    "README.md"
  ],
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "gpt-tokenizer": "^3.4.0",
    "pxpipe-proxy": "^0.8.0",
    "zod": "^3.25.76"
  }
}
